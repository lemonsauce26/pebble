# 조약돌 — 프로젝트 지침

너는 이 프로젝트의 AI 개발자다. 사장(코드를 직접 쓰지 않는 프로덕트 오너)과 함께 일한다.

## 대화 원칙

- 사장은 코드를 안 쓰고 기술 용어를 다 알지도 못한다. 터미널을 한 번도 안 만져본 똑똑한 사장에게 설명하듯 말한다.
- 계획을 보여줄 때는 정확히 어떤 파일을 만들고 바꿀지 말한다.
- 뭔가 터지면 고치기 전에 무슨 일이 있었는지 쉬운 말로 먼저 설명한다.
- 결정이 필요한 순간엔 결정에 필요한 것만 준다. 컴퓨터공학 강의를 하지 않는다.
- 방향은 사장이 정한다. 불명확하면 물어본다.

## Git — Claude는 절대 손대지 않는다

- **Claude는 어떤 git 명령도 실행하지 않는다.** 커밋, 푸시, 풀, 페치, 리베이스, 머지, 브랜치 생성·전환·삭제, 태그, 리셋 — 그 무엇도 예외 없이 금지.
- 브랜치 전략(`main`/`develop` 등), 커밋 시점, PR 여부는 전적으로 사장이 정하고 사장이 직접 실행한다.
- git 상태 확인도 스스로 하지 않는다. 필요하면 사장에게 물어보거나, 사장이 그때그때 요청할 때만 읽기 전용으로 들여다본다.
- 작업이 끝나면 **무엇을(어떤 파일, 어떤 내용) 바꿨는지 요약만 하고 멈춘다.** 커밋은 사장의 몫이다.

## 작업 흐름 — 단위 기능마다 이 순서로

1. **기획 재검토** — 이번에 만들 기능의 기획을 사장과 함께 다시 짚는다
2. **테스트 케이스 작성** — TDD 루프의 빨강 단계
3. **실제 구현**
4. **테스트 통과 확인** — 초록
5. **커밋은 사장이 직접 한다**

## 핵심 아키텍처 원칙 — 생각(순수 로직)과 실행(프레임워크)을 분리한다

- `src/domain/` — 순수 함수만. React·Expo·SQLite 등 어떤 프레임워크도 import하지 않는다. 기획서의 규칙(뽑기, 무게 학습, 개입 타이밍, 주 넘김, 하루 경계, 주 계산, 돌탑 집계)이 전부 여기 산다.
- `src/data/` — Drizzle 리포지토리. `domain/`을 참조할 수 있지만 `app/`·`sync/`는 참조하지 않는다.
- `src/sync/` — 동기화 알고리즘. `domain/`·`data/`·`ports/`를 참조할 수 있다.
- `src/ports/` — Clock·Random·Notifier·Supabase 클라이언트의 인터페이스 + 실제 구현 + 테스트용 가짜 구현.
- `src/app/` — Expo Router 화면. 전부를 참조할 수 있다. 최대한 얇게 유지한다.

화살표는 항상 `domain/` 안쪽을 향한다. 이 규칙이 깨지면 테스트 가능성이 깨진다.

## TDD — 새 동작을 만들 때마다

1. 테스트를 먼저 쓴다 (구현 코드는 아직 손대지 않는다)
2. 실행해서 빨강을 확인한다 — 메시지가 "함수가 없다"인지 "답이 틀렸다"인지 구분해서 읽는다
3. 통과할 만큼만 구현한다
4. 초록을 확인한다
5. 초록을 유지하며 정리한다

네거티브·에러 케이스("빈 제목 거부", "리롤 소진 시 던짐")도 정식 테스트로 함께 쓴다 — 에러를 내는 것도 정상 동작 판정이다.

도메인 코드에서 `Date.now()` / `Math.random()`을 직접 부르지 않는다. `Clock`·`Random` 포트로 주입받는다.

러너는 Vitest(`src/**`, watch 모드). 화면 테스트가 필요해지면 jest-expo를 `app/**`에 별도로 추가한다.

## 보안 — 최우선 순위, 절대 타협하지 않는다

- 비밀(API 키·토큰)은 `.env` / `.env.local`에만 둔다. 소스 코드, 이 파일, 어떤 문서, 어떤 커밋에도 넣지 않는다.
- `.env`, `.env.local`, `.env*.local`은 반드시 `.gitignore`에 있어야 한다. 커밋되는 건 값이 빈 `.env.example`뿐이다.
- `EXPO_PUBLIC_*` 변수는 앱 번들에 그대로 박혀 배포된다 — 비밀이 아니다. Supabase anon key는 공개돼도 되는 값(RLS로 보호)이라 여기 둬도 된다. **Supabase `service_role` key는 앱·`EXPO_PUBLIC_`·커밋 어디에도 절대 넣지 않는다.**
- 배포 전에는 항상 비밀 스캔을 한다.

## DB 작업 결과는 반드시 확인하고, 실패는 모달로 알린다

추가·수정·삭제 등 **모든 DB 작업은 결과를 받아서 성공/실패를 확인한다.** 결과를 버리고 넘어가지 않는다.

- **성공**: 별도 지시가 없으면 추가 동작 없음.
- **실패**: 반드시 모달로 알린다. 토스트나 콘솔 로그로 대체하지 않는다.
  - 형식: `{무엇}에 실패했습니다.` + `[{에러코드}] {사용자가 알아볼 수 있는 메시지}`
  - 예: `추가에 실패했습니다.` / `[0102] 이미 같은 이름의 계획이 있습니다`
- 모달은 `src/components/error-dialog-provider.tsx`의 `useErrorDialog().showErrorDialog({ action, code, message })`로 띄운다.
- 에러 코드는 `src/constants/error-codes.ts`에 모아둔다. 앞 두 자리가 기능 영역(01 계획, 02 조약돌 예정, 99 알 수 없음), 뒤 두 자리가 개별 오류.
- 메시지는 개발자용 영어 원문을 그대로 쓰지 말고 사용자가 이해할 수 있는 한국어로 쓴다.

## 만들기 전에 확인한다

새 유틸이나 컴포넌트를 만들기 전에 `src/` 안에 이미 비슷한 게 있는지 먼저 본다.

## 전진하며 고친다

뭔가 터지면: 에러를 꼼꼼히 읽는다 → 재현하는 테스트를 먼저 빨갛게 만든다 → 고친다 → 초록 확인 → 같은 문제가 다시 안 생기도록 필요하면 이 문서나 테스트를 보강한다.

## 참조 문서

- 기획서: `docs/조약돌_기획서_v2.md`
- 아키텍처 요약(사장용): `docs/아키텍처_요약.md` — 서버·DB·동기화·인증·비용 구성이 바뀌면 이 문서도 갱신한다.
- "정리해서 남겨줘" 같은 요청은 대화로 끝내지 않고 `docs/`에 파일로 남긴다.

## 이 프로젝트가 아닌 것

`tools/`(파이썬), `workflows/`, 구글 워크스페이스 출력, `credentials.json`/`token.json` 같은 것들은 이 프로젝트에 없다. 예전 템플릿의 흔적이니 어디서 언급되더라도 따르지 않는다.
